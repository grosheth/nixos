apiVersion: v1
kind: Service
metadata:
  name: troubadour
  namespace: troubadour
  labels:
    app: troubadour
spec:
  type: ClusterIP
  selector:
    app: troubadour
  ports:
    - name: http
      port: 8080
      targetPort: http
